name: flutter_projects
description: "A new Flutter project."

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.0 # 플러터에서 네트워크로 유명항 외부 api
  get: ^4.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/
#    - assets/event/ 이렇게 안 하면 assets 안에 있는 폴더 인식이 안된다고 함.